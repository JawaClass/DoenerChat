import { VoiceTranslationTable } from './voice-translation-table';

describe('VoiceTranslationTable', () => {
  it('should create an instance', () => {
    expect(new VoiceTranslationTable()).toBeTruthy();
  });
});
